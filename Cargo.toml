[package]
name = "project_name"
version = "0.1.0"
authors = []
edition = "2018"

[dependencies]
nx = { git = "https://github.com/aarch64-switch-rs/nx" }

[package.metadata.sprinkle.nsp]
npdm = "npdm.json"
